package com.ringfence.silentscheduler.core.theme

import androidx.compose.ui.graphics.Color

// Approximate tokens read off the Claude Design PDF export (no source hex available —
// the design link itself 403'd, only a rasterized print-to-PDF was provided). Treat
// these as best-effort placeholders and correct them the first time the real .dc.html
// source or a color-picked screenshot becomes available. See DESIGN_NOTES.md.

val AccentLight = Color(0xFF6C63E8)
val AccentSoftLight = Color(0xFFE7E4FB)
val BackgroundLight = Color(0xFFF7F7F9)
val SurfaceLight = Color(0xFFFFFFFF)
val OnSurfaceLight = Color(0xFF1B1B1F)
val OnSurfaceVariantLight = Color(0xFF6B6B70)
val OutlineLight = Color(0xFFE1E1E6)

val AccentDark = Color(0xFF8B83F5)
val AccentSoftDark = Color(0xFF2A2740)
val BackgroundDark = Color(0xFF121218)
val SurfaceDark = Color(0xFF1C1C24)
val OnSurfaceDark = Color(0xFFF2F2F5)
val OnSurfaceVariantDark = Color(0xFFA0A0A8)
val OutlineDark = Color(0xFF2E2E38)
