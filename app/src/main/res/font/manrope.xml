<?xml version="1.0" encoding="utf-8"?>
<!-- Manrope ships only as a variable font (single file, weight axis) — each entry
     below points at that same file with a different 'wght' axis setting rather than
     separate per-weight files, matching the weights the design source actually uses
     (400/500/600/700/800; see SilentApp.dc.html's Google Fonts <link> tag). -->
<font-family xmlns:android="http://schemas.android.com/apk/res/android">
    <font
        android:fontStyle="normal"
        android:fontWeight="400"
        android:font="@font/manrope_variable"
        android:fontVariationSettings="'wght' 400" />
    <font
        android:fontStyle="normal"
        android:fontWeight="500"
        android:font="@font/manrope_variable"
        android:fontVariationSettings="'wght' 500" />
    <font
        android:fontStyle="normal"
        android:fontWeight="600"
        android:font="@font/manrope_variable"
        android:fontVariationSettings="'wght' 600" />
    <font
        android:fontStyle="normal"
        android:fontWeight="700"
        android:font="@font/manrope_variable"
        android:fontVariationSettings="'wght' 700" />
    <font
        android:fontStyle="normal"
        android:fontWeight="800"
        android:font="@font/manrope_variable"
        android:fontVariationSettings="'wght' 800" />
</font-family>
